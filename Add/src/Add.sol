// SPDX-License-Identifier: BUSL-1.1
pragma solidity ^0.8.13;

contract Add {
        function add(uint256 a, uint256 b) public pure returns (uint256) {
        return a + b;
    }

        function testAddTwo(unit256 a, 0) public pure returns (unit256) {
        return a +0;
}

        function testAddThree(unit256 a, unit256 b, unit256 c) public pure returns (unit256) {
        return a + b
}
